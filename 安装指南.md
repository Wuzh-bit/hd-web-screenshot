# 网页截图助手 — 安装与使用指南

## 一、文件结构

```
screenshot-extension/
├── manifest.json                  # 扩展清单（Manifest V3，最小权限）
├── background.js                  # 后台 Service Worker（响应图标/快捷键并注入脚本）
├── content.js                     # 截图业务脚本（弹窗、框选、渲染、保存/复制）
├── lib/
│   └── dom-to-image-more.min.js   # 本地打包的截图渲染库（不再走 CDN）
└── icons/
    ├── icon16.png
    ├── icon48.png
    └── icon128.png
```

## 二、安装步骤（Chrome / Edge 开发者模式）

1. 打开扩展管理页：
   - **Chrome**：地址栏输入 `chrome://extensions/` 回车
   - **Edge**：地址栏输入 `edge://extensions/` 回车
2. 打开右上角（Edge 在左侧栏）的 **「开发者模式」** 开关
3. 点击 **「加载已解压的扩展程序」** 按钮
4. 选择解压后的 `screenshot-extension` 文件夹，点击「选择文件夹」
5. 工具栏出现蓝紫色相机图标即安装成功（可点拼图图标📌固定到工具栏）

> 更新代码后，回到扩展管理页点击该扩展卡片上的「刷新 ↻」即可重新加载。

## 三、使用方法

| 操作 | 方式 |
|------|------|
| 唤起截图弹窗 | 点击工具栏图标，或按 **Alt+Shift+S** |
| 区域截图 | 弹窗选「选择区域」→ 拖动框选（拖到屏幕边缘可自动滚动跨屏框选）→ 复制/保存 |
| 整页截图 | 弹窗选「整个网页」→ 自动渲染全页并弹出保存 |
| 取消 | 按 **Esc** 或点「取消」 |

快捷键冲突时可在 `chrome://extensions/shortcuts` 中自定义。

## 四、权限说明（最小化）

| 权限 | 用途 |
|------|------|
| `activeTab` | 仅在你主动点击图标/按快捷键时，临时获得当前标签页访问权 |
| `scripting` | 用于把截图脚本注入当前页面 |

**没有** `<all_urls>` 常驻注入、没有网络请求、没有数据上报，所有处理均在本地完成。

## 五、已知限制（DOM 渲染方案固有）

- `chrome://`、扩展商店等浏览器保护页面无法截图
- 无 CORS 响应头的跨域图片、`<iframe>`、视频帧、WebGL 内容可能空白
- 懒加载页面建议先滚动到底部让图片加载完，再执行整页截图
- 超大页面会自动降低清晰度倍率并提示；超出浏览器渲染上限时会建议改用区域截图
